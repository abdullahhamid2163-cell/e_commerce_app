enum TextSize { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  google, 
  applePay, 
  visa,
  masterCard,
  paystack, 
  razorPay,
  paytm,
}
